<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bcy.dao.TermDao">
    <select id="getTerms" resultType="bcy.dao.Term"> select terms.id, terms.title, terms.content, terms.ids,users.name,users.email from
        terms LEFT JOIN users ON terms.pid = users.id where pid=#{pid} 
        order by terms.createTime desc limit 0,20 
    </select>
    <insert id="addTerm" parameterType="bcy.dao.Term"> 
        insert into terms (title, content, pid, ids, type, createTime) values (#{title},#{content},#{pid},#{ids},#{type},#{createTime}) 
    </insert>

    <update id="updateTerm" parameterType="bcy.dao.Term" > 
        update terms set title=#{title}, content=#{content}, ids=#{ids} where id = #{id}
    </update>
</mapper>